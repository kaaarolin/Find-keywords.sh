# find-keywords.sh

Scans all `.sh` files in `sh-files/` modified within the last 30 days, reports every line containing `read` or `echo`, and shows how many times each word occurs.

## Project structure
